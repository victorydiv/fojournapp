# DreamHost Shared Hosting Environment Configuration Template
# Copy this file to backend/.env and fill in your actual values

# Application Environment
NODE_ENV=production
PORT=3000

# DreamHost Database Configuration
DB_HOST=mysql.yourdomain.com
DB_PORT=3306
DB_USER=victorydiv24_dbuser
DB_PASSWORD=your_secure_database_password_here
DB_NAME=victorydiv24_travel_log

# JWT Secret (use a long, random string - you can generate one with: openssl rand -base64 32)
JWT_SECRET=your_super_secret_jwt_key_here_make_it_very_long_and_random

# Google Maps API Key (get from Google Cloud Console)
# Enable: Maps JavaScript API, Geocoding API, Places API
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# File Upload Settings for DreamHost
MAX_FILE_SIZE=50000000
UPLOAD_PATH=/home/victorydiv24/yourdomain.com/fojournapp/backend/uploads

# CORS Origins (add your DreamHost domain)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# External Backend URL (if deploying backend separately)
# EXTERNAL_BACKEND_URL=https://your-railway-app.railway.app

# Optional: Email Configuration (for future features)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# DreamHost-specific notes:
# - Use your actual DreamHost username instead of "victorydiv24"
# - Database hostname is usually mysql.yourdomain.com
# - Database names are prefixed with your username
# - Upload path must be within your home directory
# - External backend recommended for full functionality
